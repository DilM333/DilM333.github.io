import type { Recipe } from '../types'

/**
 * Recipes whose primary use is a snack. Empty for now — none of the existing
 * 11 seed recipes is snack-primary. Populated in a later phase. See
 * data/recipes/index.ts.
 */
export const snackRecipes: Recipe[] = []
